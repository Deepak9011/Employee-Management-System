name: Build and deploy JAR app to Azure Web App - ems2war

on:
  push:
    branches:
      - version
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java version
        uses: actions/setup-java@v2  # Use latest stable version
        with:
          java-version: '21'

      - name: Build with Maven
        run: mvn clean package  # Build the artifact (instead of install)

      - name: Upload artifact for deployment job
        uses: actions/upload-artifact@v3
        with:
          name: java-app
          path: './target/*.jar'  # Correct path for uploaded artifact

  deploy:
    runs-on: windows-latest
    needs: build
    environment:
      name: 'Production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
      - name: Download artifact from build job
        uses: actions/download-artifact@v3
        with:
          name: java-app

      - name: Deploy to Azure Web App
        id: deploy-to-webapp
        uses: azure/webapps-deploy@v2
        with:
          app-name: 'ems2war'  # Ensure this is correct
          slot-name: 'Production'  # Specify your deployment slot
          publish-profile: ${{ secrets.AZUREAPPSERVICE_PUBLISHPROFILE_C1A708DD697D4C6C930B42270531CFCE }}  # Ensure publish profile is correct
          package: './target/*.jar'  # Correct path to the downloaded artifact
